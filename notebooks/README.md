# Notebooks

| Notebook | Purpose |
|---|---|
| `01_eda.ipynb` | Data loading, raw price and log-return plots, event overlay, stationarity (ADF) tests, volatility analysis (Task 1 / Task 2.1) |
| `02_changepoint_model.ipynb` | Bayesian change point model in PyMC, convergence diagnostics, posterior interpretation, event association (Task 2) |

Run from the repo root with the project virtualenv active so that
`src/` imports resolve.
